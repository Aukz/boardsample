<table class="bg-white table table-bordered mb-0 mt-3">
  <tbody>
      <% @sureds.each do |sured| %>
        <tr style="border: 2px solid #d2d2d2;">
          <td scope="row">
            <div class="pl-5">
              <h4><%= link_to sured.title, sured%><span class="ml-3 h6">作者: <%= sured.editer %></span></h4>
            </div>
          </td>
        </tr>
      <% end %>
  </tbody>
</table>
